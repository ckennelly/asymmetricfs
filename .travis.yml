language: cpp

compiler:
  - clang
  - gcc

before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq cmake libboost-all-dev libfuse-dev libgtest-dev

before_script:
- mkdir build
- cd build

script:
- cmake .. && cmake --build .

after_script:
- ctest -V .
